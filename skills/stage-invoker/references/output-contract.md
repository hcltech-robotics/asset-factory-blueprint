# Output contract

Return ToolResult with success, data, error, warnings, artefacts, proposals and validation_status. The data block carries stage_id, final_state, the iteration record and the stage report, progress and contact sheet paths.
