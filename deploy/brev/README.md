# Brev

The Brev launch command performs environment and provider discovery. It is a finite readiness check rather than a persistent tool service.

Pin the repository revision and machine image in the Brev environment, then inject provider credentials through Brev secrets. Run the documented dry run only after the provider report and capability report have been retained.
