# Osmo

This directory describes the finite validation and dry-run commands to adapt into the OSMO workflow version installed at a site. It is not an HTTP service definition and contains no credentials.

Pin the source image, mount a writable project and artefact store and inject provider secrets through the site secret manager. Preserve the skill audit and dry-run workspace as workflow outputs.
